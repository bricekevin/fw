# Design System

**Project:** [PROJECT_NAME]

---

## Brand Colors

```css
--primary: [HEX];
--secondary: [HEX];
--accent: [HEX];

--success: [HEX];
--warning: [HEX];
--error: [HEX];
```

---

## Typography

```css
--font-primary: [FONT_NAME];
--font-mono: [MONO_FONT];

--text-xs: [SIZE];
--text-sm: [SIZE];
--text-base: [SIZE];
--text-lg: [SIZE];
--text-xl: [SIZE];
```

---

## Spacing

```css
--space-1: [VALUE];  /* Small */
--space-2: [VALUE];  /* Medium */
--space-3: [VALUE];  /* Large */
```

---

## Components

### Buttons
```css
.btn-primary { [STYLES] }
.btn-secondary { [STYLES] }
```

### Forms
```css
.input { [STYLES] }
.select { [STYLES] }
```

### Cards
```css
.card { [STYLES] }
```

---

## Responsive Breakpoints

```css
--mobile: [WIDTH];
--tablet: [WIDTH];
--desktop: [WIDTH];
```

---

**Last Updated:** [DATE]

